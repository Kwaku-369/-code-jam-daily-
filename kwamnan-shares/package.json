{
  "name": "kwamnan-shares",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "web": "cd apps/web && npm run dev",
    "worker": "cd apps/worker && wrangler dev"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.4.5"
  },
  "engines": { "node": ">=20" }
}
